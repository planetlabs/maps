---
title: 'Popup'
level: 1
description: |
  This example shows how to use the `<Overlay>` component to create
  a popup.  The popup is shown when you click on the map.  Click on the
  popup to close it.
  In this example, a `<div>` element is used to render the popup content.
  You can use any other React component to render the popup.
---

import Popup from '../../components/examples/Popup.jsx';

<Popup client:only="react" />
