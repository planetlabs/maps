---
title: 'Slider Compare'
level: 1
description: |
  This example shows a range slider can be used to compare two layers.  The
  `onPrerender` and `onPostrender` event listeners are used to clip the top
  layer based on the slider position.
---

import Slider from '../../components/examples/Slider.jsx';

<Slider client:only="react" />
